import { useRouter } from "next/router";
import { ChangeEvent, useEffect, useState } from "react";
import styled from "styled-components";

const movieImage = ["데시벨.jpeg","동감.jpeg","에브리씽.webp","올빼미.jpeg","자백.jpeg","짱구.png","폴.jpeg"]
const movieTitle = ["데시벨", "동감", "에브리씽", "올빼미"]
export default function Movie() {
  return (
    <article>
      {/* {movieImage.map(i => <img key ={1} src={i} alt={i} width={100} height={200} />)} */}
      <h2></h2>
      <div></div>
    </article>
  );
}
//jsx에서 js 코드 쓰려면 중괄호를 쓴다~
